# Mobile production env — committed so external users can build a personal
# iPhone copy of Chimii against the same backend as chimii.ai on web.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://api.chimii.ai
EXPO_PUBLIC_WEB_URL=https://chimii.ai
